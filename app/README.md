# Desktop App

Tauri + Svelte + TypeScript desktop app for Pardus Mihmandar.

## Run

```powershell
npm install
npm run tauri dev
```

## Safety

The UI includes a command safety review component. AI-generated commands must never execute automatically.
